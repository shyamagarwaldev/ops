apiVersion: v1
kind: Service
metadata: 
    name: backend
    namespace: {{ .Values.backend.namespace }}
spec: 
    type: ClusterIP
    ports:
        - targetPort: {{ .Values.backend.port }}
          port: {{ .Values.backend.service.port }}
          protocol: TCP
    selector: 
        app: backend      
