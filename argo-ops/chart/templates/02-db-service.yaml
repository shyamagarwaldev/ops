apiVersion: v1
kind: Service
metadata:
    name: db
    namespace: {{ .Values.db.namespace }}
spec:
    type: ClusterIP
    selector:
        app: db
    ports: 
        - targetPort: {{ .Values.db.port }}
          port: {{ .Values.db.service.port }}
          protocol: TCP


